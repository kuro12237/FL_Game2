#pragma once

#include <memory>
#include <string>
#include <vector>

class TitleSpriteManager
{
 public:
   TitleSpriteManager() = default;
   ~TitleSpriteManager() = default;

   void Init();
   void Update();
   void Clear();

 private:
};
